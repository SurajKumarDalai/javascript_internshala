// Rules for JavaScript Identifiers

// An identifier must follow these rules:

// Can contain letters, digits, underscores (_), and dollar signs ($)

// Must start with a letter, _, or $ (not a digit)

// Case-sensitive (total ≠ Total)

// Cannot be a reserved keyword (like let, if, class)

// No spaces allowed


let age;
let userName;
let _total;
let $price;
let user1;

